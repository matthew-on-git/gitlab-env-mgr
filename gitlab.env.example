# GitLab Environment Configuration
# Copy this to gitlab.env and fill in your values

# GitLab instance URL
GITLAB_URL=https://gitlab.example.com

# GitLab personal access token with api scope
# Create at: https://gitlab.example.com/-/profile/personal_access_tokens
GITLAB_TOKEN=your-personal-access-token-here

# Optional: Default project ID (can be overridden with -p flag)
# GITLAB_PROJECT_ID=hardware-infra/imp
